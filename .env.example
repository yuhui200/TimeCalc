# 构建目标：web | tauri | capacitor
# web       -> 启用 PWA（Service Worker + manifest），base = /
# tauri     -> 关闭 PWA，base = /（自定义协议加载）
# capacitor -> 关闭 PWA，base = ./（file:// 加载，需相对路径）
VITE_TARGET=web

# 应用版本号展示
VITE_APP_VERSION=0.1.0
